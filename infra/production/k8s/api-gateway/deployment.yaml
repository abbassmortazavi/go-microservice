apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: abbass69/api-gateway:dev
          imagePullPolicy: Always
          ports:
            - containerPort: 8085
          env:
            - name: GATEWAY_HTTP_ADDR
              value: ":8085"
            - name: ENVIRONMENT
              value: "development"
            - name: AUTH_SERVICE_URL
              value: "auth-service:9092"