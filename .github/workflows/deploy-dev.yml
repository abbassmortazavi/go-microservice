name: Deploy to Development

concurrency:
  group: development-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["develop", "feature/**", "bugfix/**"]

# 🔥 تنظیم permissions در این سطح
permissions:
  contents: write  # برای push تغییرات
  packages: write  # برای push به Docker Hub
  id-token: write  # برای authentication

jobs:
  # مرحله ۱: تست
  tests:
    uses: ./.github/workflows/run-tests.yml
    secrets: inherit

  # مرحله ۲: ساخت images
  build:
    uses: ./.github/workflows/run-build.yml
    needs: tests
    secrets: inherit

  # مرحله ۳: دیپلوی
  deploy:
    uses: ./.github/workflows/deploy.yml
    needs: build
    secrets: inherit
    with:
      environment: 'development'