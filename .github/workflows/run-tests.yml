name: Run Tests

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
        default: 'production'

env:
  GO_VERSION: '1.21'

jobs:
  unit-tests:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Run tests if any
        run: |
          echo "🧪 Checking for Go tests..."
          
          # اجرای تست‌ها در کل پروژه
          if go test ./... -v; then
            echo "✅ Tests passed!"
          else
            # اگر exit code 1 باشد یعنی تست‌ها fail شده‌اند
            # اگر exit code دیگر باشد احتمالاً تستی وجود ندارد
            TEST_EXIT_CODE=$?
            if [ $TEST_EXIT_CODE -eq 1 ]; then
              echo "❌ Tests failed!"
              exit 1
            else
              echo "⚠️ No tests to run (exit code: $TEST_EXIT_CODE)"
              echo "✅ Continuing without tests"
            fi
          fi